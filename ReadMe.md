# 🎬 YouTube Video Downloader

دانلودر سریع و متن‌باز برای ویدیوهای یوتیوب با پشتیبانی از پلی‌لیست، دانلود صفی و تشخیص خودکار پروکسی. این پروژه برای استفاده‌ی روزمره، اسکریپت‌نویسی و محیط‌های محدود طراحی شده و تمرکزش روی سادگی، سرعت و پایداریه.

> ⚠️ این پروژه **غیررسمی** است و وابستگی مستقیمی به YouTube ندارد.

---

## 📌 فهرست مطالب

* [ویژگی‌ها](#-ویژگیها)
* [پیش‌نیازها](#-پیشنیازها)
* [نصب](#-نصب)
* [استفاده](#-استفاده)
* [تنظیمات پیشرفته](#-تنظیمات-پیشرفته)
* [مثال‌های پرکاربرد](#-مثالهای-پرکاربرد)
* [سؤالات متداول](#-سؤالات-متداول)
* [مشارکت در پروژه](#-مشارکت-در-پروژه)
* [مجوز](#-مجوز)

---

## ✨ ویژگی‌ها

* ⚡ دانلود سریع با فرگمنت‌های همزمان
* 📋 پشتیبانی کامل از پلی‌لیست‌ها
* 🔗 دانلود تکی یا چندتایی
* 🌐 تشخیص خودکار پروکسی (V2Ray، Clash و مشابه آن‌ها)
* 🎥 انتخاب کیفیت خروجی از 360p تا 1080p
* 🎵 امکان دانلود فقط صدا
* 📊 نمایش زنده‌ی پیشرفت، سرعت و زمان باقی‌مانده
* 📋 گزارش نهایی دانلودها
* 🔄 تلاش مجدد خودکار در صورت خطا
* 🚫 بدون نیاز به ffmpeg

---

## 📦 پیش‌نیازها

* Python نسخه **3.10 یا بالاتر**
* مدیر پکیج **uv**

بررسی نسخه پایتون:

```bash
python --version
```

---

## 📥 نصب

### 1. نصب uv

**Linux / macOS**

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Windows (PowerShell)**

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 2. دریافت پروژه و نصب وابستگی‌ها

```bash
git clone https://github.com/USERNAME/REPOSITORY.git
cd REPOSITORY
uv sync
```

### 3. بررسی نصب صحیح

```bash
uv run yt-downloader --help
```

---

## ▶️ استفاده

### استفاده از فایل لینک‌ها

لینک‌ها را در فایل `links.txt` قرار دهید:

```text
https://www.youtube.com/watch?v=VIDEO_ID
https://youtu.be/VIDEO_ID_2
https://www.youtube.com/playlist?list=PLxxxx
```

سپس اجرا کنید:

```bash
uv run yt-downloader -f links.txt
```

### لینک مستقیم

```bash
uv run yt-downloader -u "https://www.youtube.com/watch?v=VIDEO_ID"
```

---

## 🎥 تنظیم کیفیت خروجی

```bash
# پیش‌فرض (720p)
uv run yt-downloader -f links.txt

# 360p (کم‌حجم)
uv run yt-downloader -f links.txt --format "best[height<=360]/best"

# 1080p (حداکثر کیفیت بدون ffmpeg)
uv run yt-downloader -f links.txt --format "best[height<=1080]/best"
```

---

## ⚡ تنظیمات پیشرفته

### تعداد فرگمنت‌های همزمان

```bash
uv run yt-downloader -f links.txt -j 8
```

### تنظیم پروکسی

```bash
# تشخیص خودکار
uv run yt-downloader -f links.txt

# SOCKS5
uv run yt-downloader -f links.txt --proxy socks5://127.0.0.1:1080

# بدون پروکسی
uv run yt-downloader -f links.txt --no-proxy
```

---

## 🏆 مثال‌های پرکاربرد

```bash
# دانلود سریع 1080p
uv run yt-downloader -f links.txt -j 8 --format "best[height<=1080]/best"

# دانلود فقط صدا
uv run yt-downloader -f links.txt --format "bestaudio/best" -o ./music
```

---

## ❓ سؤالات متداول

**آیا این ابزار نیاز به VPN دارد؟**
در صورت محدودیت منطقه‌ای، ابزار می‌تواند پروکسی را به‌صورت خودکار یا دستی استفاده کند.

**آیا دانلود 4K پشتیبانی می‌شود؟**
در حال حاضر تمرکز پروژه روی دانلود پایدار تا 1080p بدون وابستگی به ffmpeg است.

---

## 🤝 مشارکت در پروژه

مشارکت‌ها کاملاً خوش‌آمد هستند.

1. Fork کنید
2. یک Branch جدید بسازید
3. تغییرات را Commit کنید
4. Pull Request ارسال کنید

پیشنهادها، باگ‌ریپورت و بهبود مستندات ارزشمند هستند.

---

## 📄 مجوز

این پروژه تحت مجوز **MIT** منتشر شده است. استفاده، تغییر و توزیع آن آزاد است، به شرط حفظ حق کپی‌رایت.
